#include <stdio.h>
#include <conio.h>

int main ()
{
    int P,Q,Total;
    printf("Enter the value of P :");
    scanf("%d", &P);
    printf("Enter the value of Q :");
    scanf("%d", &Q);
    Total = P + Q;
    printf("%d", Total);
    return 0;
}
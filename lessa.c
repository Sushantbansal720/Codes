#include <stdio.h>

int main ()
{
    int a,b;

    printf("Enter a value for a:");
    scanf("%d", &a);

    printf("Enter a value for b:");
    scanf("%d", &b);

    if (a<b)
    {
        printf("a's is a less than for b's value");
    }
    else
    {
        printf("b's is a less than for a's value");
    }
    
    
    
}